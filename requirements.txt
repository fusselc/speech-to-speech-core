# Core audio recording
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.11.0

# Speech-to-text (Whisper)
# torch is an explicit dependency of openai-whisper; listing it here ensures
# the correct version is resolved when installing in a fresh environment.
torch>=2.0.0
openai-whisper>=20231117

# Text-to-speech (local, offline)
# NOTE: pyttsx3 is a placeholder. Replace with OpenVoice for higher-quality synthesis.
pyttsx3>=2.90
